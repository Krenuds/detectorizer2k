THIS SUB IS INFESTED = TRUE

## What is this?

The Detectorizer 2000 has detected that **r/YourFavoriteNicheSubreddit** has been invaded and taken over by normies. Brace yourselves — we now all have to deal with their discovery process all over again for the next year. The "has anyone else tried..." posts. The "unpopular opinion but..." threads about the most popular opinion possible. The screenshots of things we've all seen a thousand times, posted like they just unearthed the Dead Sea Scrolls. Godspeed.

## Configuration

```json
{
  "mcpServers": {
    "normie-detector": {
      "command": "npx",
      "args": ["-y", "detectorizer2k"],
      "env": {
        "INFESTATION_LEVEL": "CRITICAL",
        "COPIUM_RESERVES": "DEPLETED",
        "REPOST_TOLERANCE": "0",
        "GATEKEEPING_MODE": "MAXIMUM",
        "EM_DASH_RESERVE": "———————————————————————————————————",
        "EM_DASH_RELOAD_ON_EMPTY": true,
        "EM_DASH_RATIONING": false
      }
    }
  }
}
```

> **Note:** The `EM_DASH_RESERVE` must be kept fully stocked at all times. Claude will burn through your entire supply in a single paragraph — and then — just when you think it's over — it hits you with three more. Rationing has been disabled because it simply does not work. We tried.
```
